# grub.conf generated by anaconda
#
# Note that you do not have to rerun grub after making changes to this file
# NOTICE:  You do not have a /boot partition.  This means that
#          all kernel and initrd paths are relative to /, eg.
#          root (hd0,3)
#          kernel /boot/vmlinuz-version ro root=/dev/sda4
#          initrd /boot/initrd-version.img
#boot=/dev/sda
default=0
timeout=0
hiddenmenu
serial --unit=0 --speed=115200 --word=8 --stop=1
terminal serial
setled --color=RED --status=ON

title USB-Rescue 
	root (hd0,0)
	kernel (hd0,0)/bzImage root=(hd0,0) ro console=ttyS0,115200n8 quiet
	initrd (hd0,0)/usbinitramfs.gz
